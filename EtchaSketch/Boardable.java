/**
 * specifies what Stylus does
 * Stylus must have method isVisible
 * 
 * @author jessicaoathoudt
 *
 */
public interface Boardable 
{
	/**
	 * 
	 * @return boolean - like a light switch - true if elem is visible
	 * 
	 */
	public boolean isVisible();
	
}